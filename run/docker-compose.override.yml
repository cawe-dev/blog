version: '3.9'
services:
  mailpit:
    image: axllent/mailpit
    container_name: cawe_blog_mailpit
    ports:
      - "${MAIL_DASHBOARD_PORT:-8025}:8025"
      - "${MAIL_PORT:-1025}:1025"

  postgres:
    ports:
      - "${DB_PORT:-5432}:5432"
